environment: prod
mqtt:
  host: mqtt.avinent.local
  port: 8883
  tls_enabled: true
  ca_cert_path: /app/certs/ca.crt
polling:
  interval_seconds: 2
heartbeat:
  interval_seconds: 30
logging:
  level: INFO
